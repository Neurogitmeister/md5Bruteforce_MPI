-- функция проверки хэша с массивом хэшей (слив базы с сайта)

-- версии компиляции с массивом хэшей и без массива

-- попытаться не загружать всё время остатками от вычислений начальные процессы, может быть счётчик последнего использованного ранга поможет
но тогда снова пересчитывать вывод итерометра -_____________-
мб ну его нахуй
